\subsection{Methods: heme oxidation enzyme}
\label{sec:met:heme}

\plan{Application-specific methods. Cross-reference
Section~\ref{sec:met:overview} for shared pipeline details.}

\begin{planlist}
  \item \textbf{Block 1 --- Design configuration.} Backbone, fixed residues,
        reward pipeline configuration.
  \item \textbf{Block 2 --- Three-arm comparison protocol.} SSM library
        construction; vanilla MPNN re-sampling protocol (temperature, fixed
        residues); fine-tuned MPNN + fragment library; matched-budget rule ---
        how $N$ was chosen and held constant across arms.
  \item \textbf{Block 3 --- Activity assay.}
\end{planlist}
\todo{heme methods --- design configuration}
\todo{heme methods --- three-arm protocol}
\todo{heme methods --- assay description}
