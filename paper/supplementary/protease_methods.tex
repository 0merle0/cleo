\subsection{Methods: protease}
\label{sec:met:protease}

\begin{planlist}
  \item \textbf{Block 1 --- Design configuration.} Backbone, fixed residues.
  \item \textbf{Block 2 --- Two-arm protocol.} Arm (i): standard reward set.
        Arm (ii): same plus the \texttt{mutation\_diversity} reward. Record
        which variant was used --- \texttt{consensus\_divergence} or
        \texttt{fractional\_divergence} --- and the weight chosen. State the
        matched-budget rule.
  \item \textbf{Block 3 --- Activity assay.}
\end{planlist}
\todo{protease methods --- design configuration}
\todo{protease methods --- ablation protocol}
\todo{protease methods --- assay description}
