% --- Methods: Protease ---

% Block 1 — Backbone, fixed residues.
\todo{protease methods — design configuration}

% Block 2 — Two-arm protocol.
%   - Arm (i): standard reward set.
%   - Arm (ii): same + mutation_diversity reward (which variant —
%     consensus_divergence vs fractional_divergence — and the weight chosen).
%   - Matched-budget rule.
\todo{protease methods — ablation protocol}

% Block 3 — Activity assay.
\todo{protease methods — assay description}
