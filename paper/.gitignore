# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.nav
*.snm
*.vrb
*.lof
*.lot
*.run.xml
*.bcf

# Compiled PDF output
# main.pdf is intentionally TRACKED: the build_and_publish.sh hook commits it
# to origin/paper so collaborators can read the current draft without a TeX
# install. Other PDF output stays untracked.
supplement.pdf

# Hook build log
.build.log

# SVG → PDF build output
figures/pdf/

# Editor / OS noise
.DS_Store
*.swp
